{
	"version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run Tests",
            "type": "shell",
            "command": "docker build -t laravel-translatable-test . && docker run --rm laravel-translatable-test",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
